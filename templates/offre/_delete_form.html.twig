<form method="post" action="{{ path('app_offre_delete', {'id': offre.id}) }}" onsubmit="Êtes-vous sûr de vouloir supprimer cette offre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.id) }}">
    <button class="btn-rouge w-100 mt-2">Supprimer</button>
</form>
