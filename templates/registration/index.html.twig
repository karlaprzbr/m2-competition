{% extends 'base.html.twig' %}

{% block title %}Hello LoginController!{% endblock %}

{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <div class="row">
        <div class="col-6">
            <a href="{{  path('app_register', {role:'ROLE_CAN'}) }}">
                <div class="card">
                    <h4 class="card-title">Candidat</h4>
                </div>
            </a>
        </div>
        <div class="col-6">
            <a href="{{  path('app_register', {role:'ROLE_ENT'}) }}">
                <div class="card">
                    <h4 class="card-title">Entreprise</h4>
                </div>
            </a>
        </div>
    </div>
{% endblock %}
