{% extends 'base.html.twig' %}

{% block title %}Diplome index{% endblock %}

{% block body %}
    <h1>Diplome index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Niveau</th>
                <th>Titre</th>
                <th>Description</th>
                <th>DateDebut</th>
                <th>DateFin</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for diplome in diplomes %}
            <tr>
                <td>{{ diplome.id }}</td>
                <td>{{ diplome.niveau }}</td>
                <td>{{ diplome.titre }}</td>
                <td>{{ diplome.description }}</td>
                <td>{{ diplome.dateDebut ? diplome.dateDebut|date('Y-m-d') : '' }}</td>
                <td>{{ diplome.dateFin ? diplome.dateFin|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_diplome_show', {'id': diplome.id}) }}">show</a>
                    <a href="{{ path('app_diplome_edit', {'id': diplome.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_diplome_new') }}">Create new</a>
{% endblock %}
